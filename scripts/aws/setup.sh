# For package type "Image"
sudo apt install --no-install-recommends --assume-yes docker.io

# Install & configure AWS CLI
sudo apt-get -y install --no-install-recommends --assume-yes awscli
sudo aws configure